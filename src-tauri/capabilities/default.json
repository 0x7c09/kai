{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the desktop pet window.",
  "windows": ["pet"],
  "permissions": ["core:default"]
}
